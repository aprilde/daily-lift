# 3 - Build

The actual work to build the MVP: the native Android app (Kotlin + Jetpack Compose) and the home screen widget, through to an installable result.

**Status: in progress.** Right now this folder holds the *draft* build plan (`stage3-build-prompt-DRAFT.md`), which is the input to the team review in `../2-team-review/`. After the review hardens it, the final build spec and the native app work will live here.
